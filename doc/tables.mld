article (id SERIAL NOT NULL, title TEXT NOT NULL, image VARCHAR NOT NULL, text TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL, updated_at TIMESTAMPTZ NULL)
country (id SERIAL NOT NULL, name TEXT NOT NULL, color TEXT NOT NULL)
article_has_country (#article(id) INT NOT NULL, #country(id) INT NOT NULL)